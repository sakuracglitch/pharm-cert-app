# 認定資格・単位管理 PWA v1.19.2

v0.13を基準に、日常利用できる完成版として整理したPWAです。

## 基本機能

- アプリ名・画面上の試作版表記を整理
- PWAとしてホーム画面から独立表示
- オフライン起動用Service Worker
- 研修・単位・設定データの主保存先をIndexedDBへ移行
- v0.13までのlocalStorageデータを初回起動時に自動移行
- データ形式のバージョン管理（schemaVersion）
- データ形式更新時の移行前スナップショットをIndexedDB内に保持
- JSONバックアップ書き出し / 読み込み
- 同じURLでアプリ本体を更新した場合、入力済みデータを維持
- 設定画面でアプリバージョンと保存方式を確認可能
- Service Workerはオンライン時に最新版を優先し、オフライン時はキャッシュを利用

## データ更新の考え方

アプリ本体とユーザーデータを分離しています。

1. アプリ本体をv1.1、v1.2…へ更新
2. 同じURLへ新しいファイルを配置
3. 端末内のIndexedDBはそのまま維持
4. データ構造が変わる場合は `schemaVersion` を上げ、`migrateData()` で旧データを変換

v0.13までのデータが同じブラウザ・同じURLのlocalStorageに残っている場合、v1.1初回起動時に自動でIndexedDBへ移します。

## バックアップ

設定 → データ → 「バックアップを書き出す」からJSONを保存できます。
機種変更や大きな更新の前には、念のためJSONバックアップを1つ取る運用を推奨します。

## PWAとして利用する条件

PWAのService WorkerはHTTPS（またはlocalhost）で配信した場合に動作します。
固定URLへこのフォルダ一式を配置し、iPhoneのSafariで開いて「ホーム画面に追加」してください。

## 現在の機能

- 病院薬学認定薬剤師の単位・年度・領域・項目管理
- 研修記録 / 編集 / 履歴
- 項目別取得一覧
- 申請計画（開始年度 → 対象3年度 → 申請予定年度）
- 単位配分シミュレーション / 手動配分 / 一括確定
- HOPESS照合ON/OFF設定
- HOPESS / シクミネット / 日病薬公式リンク
- ボルドー / ティールテーマ、ダークモード
- 目標資格の追加用拡張枠
- 学会プログラム登録UI（自動OCRは今後更新対象）

## 注意

HOPESSの自動ログインや非公開APIへの接続は行いません。
画像（PNG/JPEG/WebP）はTesseract.jsによる日本語+英語OCRに対応します。読取結果は必ず元資料と照合してください。


## PDF・画像読取
- テキスト情報を含むPDFはブラウザ内で解析し、I-1〜V-3と単位数を候補として抽出します。
- 元PDFの掲載順を維持します。
- 自動認識できない区分・単位数は行ごとの「編集」から手動修正できます。
- PNG/JPEG/WebP等の画像はOCR対象です。画像スキャンだけのPDFはページ数が多い場合の負荷を避けるため自動OCR対象外です。必要ページを画像として読み込んでください。
- PDF.js はCDNから読み込みます。PDFファイルそのものを外部サーバーへ送信する処理はありません。


### 読取改善
- 表で「1単位」が研修項目コードの左側にある場合も拾えるように単位対応付けを改善しました。
- 単位数が前後の行に分離された場合も近接行から補完します。
- 画像OCRは初回にOCR用データを取得するため通信環境によって時間がかかります。


### 表形式PDFの単位継承
- 表の各行に単位数が繰り返されず、セクション見出しに「単位数：各1単位」「各0.5単位」とまとめて記載されるPDFに対応しました。
- セクション見出しの単位数を、そのセクション内で単位数が省略された研修項目へ自動適用します。
- 行内に0.5単位等が明記された例外（複数区分を含む行）は、行内の明記を優先します。


### v1.6 単位表ページの優先解析
- PDF全ページをそのまま研修一覧化せず、「単位に関するご案内」「研修項目」「病院薬学単位・研修科目」「単位数」などを手掛かりに単位表ページを探します。
- 「単位に関するご案内」を検出した場合、その直後の単位表ページを優先し、抄録本文に入った後は解析を打ち切ることで誤認識と処理負荷を減らします。
- 「単位数：各1単位」「単位数：各0.5単位」のようなセクション共通単位を各行に引き継ぎます。
- 行内に `II-4 0.5単位 / II-5 0.5単位` のような個別指定がある場合は、共通単位より個別指定を優先します。
- 日本語ローマ数字（Ⅰ〜Ⅴ）、全角ダッシュ、表内で前後行に分離した単位表記を正規化して読み取ります。



## v1.6 追加
- 画像OCRに失敗した場合、アップロード画像を記録画面上部に固定表示したまま入力できます。
- 取り込んだ学会プログラムを端末内に保存し、「取り込み済みの学会」から後日続きから開けます。
- 参加/不参加の状態を自動保存。登録済み講演は登録日付きでロックし、同じ講演の二重登録を防ぎます。
- 1日目を登録した後も学会データを残し、取得日を2日目に変更して未登録の参加講演だけ追加登録できます。
- 元PDF/画像はIndexedDBのファイル領域に保存します。JSONバックアップには元ファイル本体は含みませんが、学会の解析結果・参加状況は含まれます。
- ホームの「現在の取得状況」にあった重複導線「配分を見る」を削除。「おすすめ配分」は残しています。


## v1.8 変更
- 履歴画面の「選択」と各カード内の「編集」ボタンを廃止しました。
- 研修履歴カードは枠内のどこをタップしても編集画面へ移動します。
- 編集画面の「変更を保存」の下に「全部削除」を追加しました。削除前に確認を表示します。
- 研修を全部削除すると、その研修に紐づく取得単位・配分情報も削除します。
- 学会プログラムから登録した研修を全部削除した場合は、該当講演の「登録済み」状態も解除し、必要なら再登録できるようにします。
- UI上の「手入力」表記を「入力」に統一しました。


## v1.9
- 起動時・バックグラウンド復帰時に `version.json` を自動確認。入力途中でなければ新版を自動適用します。
- 入力・編集途中は再読み込みせず、保存後に自動更新します。設定から手動で「更新を確認」も可能です。
- PDF解析は目次を先に確認し、「単位に関するご案内」「認定単位」「研修単位」等のページ番号を見つけて、そのページ周辺を優先解析します。目次で特定できない場合のみPDF全体検索へフォールバックします。
- PDF.js / Tesseract.js は必要時に複数CDNから読み込むフォールバック方式に変更し、1つのCDN障害で解析不能になりにくくしました。
- 画像OCRはグレースケール・コントラスト補正・適度な拡大をしてから解析し、結果が取れない場合は元画像でも再試行します。


## v1.10
- 「解析結果のサンプルを見る」を削除し、実際のPDF・画像取り込みにUIを整理しました。
- 学会プログラムから単位を登録した直後に、取り込みデータを「残してあとで続ける」か「入力完了・学会データを削除」か選べます。
- 学会データを削除しても、登録済みの研修・単位は履歴に残ります。
- 学会データを削除した場合は、保存していた元PDF・画像も端末内ストレージから削除します。


## v1.11.0
- 項目別一覧のUIを改善（列幅・高さ・整列の見直し）
- 項目名タップで領域要件のポップアップを表示
- 学会登録の下部サマリーカードの文言と位置を改善
- 更新完了トーストを短く表示するよう調整


## v1.12.0
- ダークモードで未達成行が白く浮く問題を修正
- ティールテーマのダークモードでヘッダー・未達成行・関連UIを暗色に統一
- 未達成は背景ではなくアクセント色の数値で識別しやすく調整


## v1.13.0
- 指定されたUI整理のみ反映：入力画面の補助行削除、資料表記統一、不参加ボタン削除、取り込み済み件数整理、配分見出し変更、項目列表示調整、画像参照位置保持。


## v1.16
- 配分シミュレーションを認定要件ベースのおすすめ配分に更新。確定済みを固定し、必須項目・領域要件・年度要件・総50単位を満たすために必要な組み合わせを優先し、余剰分を未割当にします。
- 同条件では古い単位を優先し、日病薬以外のCPC対象単位は10単位上限を考慮します。
- ホームの配分シミュレーションから説明文・年度分表示を削除。
- 履歴カードの「○明細」を削除。
- 項目別一覧は項目列を維持しつつ、研修列を圧縮して研修名を1行省略表示にしました。
- 決定済みの進捗リング＋グリッドのアイコンへ変更しました。


## v1.17
- 承認済みの文言整理のみ反映。STATUS / REQUIREMENT、指定された説明文、未割当説明枠を削除。
- 履歴は項目別一覧を最初に表示。
- 参加予定の登録を追加（取得単位には加算しない）。
- 同一日・同一研修名の新規登録は単位明細を合算。
- PDF・画像取り込み時の登録名を編集可能に変更。
- Excel（.xlsx / .csv。旧 .xls はライブラリが読める範囲）から取得日・研修名・単位・項目を候補として読み込み、確認後に登録できる機能を追加。

## v1.19
- 記録画面を整理し、ファイル取り込みと入力を同じ「記録」画面内の独立した枠として配置。
- PDF・画像・Excel・CSVは一つのファイル取り込み入口から自動判別して処理。
- 未来日付の登録は予定として保持し、取得単位・進捗には加算しない。
- 当日以降の未処理予定を記録画面上部に表示し、内容確認後に「参加済み」にして取得記録へ移行。
- 未来の予定はホームの「今後の予定」から編集可能。
- 記録画面下部の予定一覧を削除し、予定管理の導線を整理。
- 項目別取得一覧の操作説明文を削除。
- 記録画面のファイル取り込みUIを中央揃えの小さなイラスト付き導線に整理。
- 設定画面の端末保存・同一URL引き継ぎ説明文を削除。



## v1.19.2 緊急復旧

- v1.19.1でプレビュー由来IDだけを条件に削除していた処理を廃止しました。
- プレビューの元内容と完全一致するレコードだけをサンプルとして除外します。
- v1.19.1更新直前に自動保存されたschema 11移行バックアップが残っている場合、プレビューIDを持つが内容が編集済みだった研修・予定だけを一度だけ復元します。
- 現在保存されている他の研修・予定は上書きしません。
